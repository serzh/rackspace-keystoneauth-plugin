============
Installation
============

At the command line::

    $ pip install python-rackspace-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-rackspace-auth
    $ pip install python-rackspace-auth
